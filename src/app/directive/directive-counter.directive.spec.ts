import { DirectiveCounterDirective } from './directive-counter.directive';

describe('DirectiveCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveCounterDirective();
    expect(directive).toBeTruthy();
  });
});
